{
  "name": "age_calculator",
  "description": "Calculate age in full years from a birthdate.",
  "input_schema": {
    "type": "object",
    "properties": {
      "birthdate": {
        "type": "string"
      },
      "fmt": {
        "type": "string",
        "description": "Optional datetime format (default %Y-%m-%d)."
      }
    },
    "required": [
      "birthdate"
    ]
  },
  "execution_function": "age_calculator"
}
{
  "name": "random_choice",
  "description": "Choose a random item from a list of strings.",
  "input_schema": {
    "type": "object",
    "properties": {
      "options": {
        "type": "array",
        "minItems": 1,
        "items": {
          "type": "string"
        }
      }
    },
    "required": [
      "options"
    ]
  },
  "execution_function": "random_choice"
}